export default function Settings() {
  return (
    <div className="page">
      <section className="card">
        <h2 className="card-title">Settings</h2>
        <p className="muted">
          Settings are not configurable in this version.
        </p>
      </section>
    </div>
  );
}
